 let t = true in
	 let f = false in 
    	 let x = port in
     		thread {send x t};
     			let prova = {receive x} in
     				skip
     			end
     		 end;
     		{send x f}
     	end
     end
end
